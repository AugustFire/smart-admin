<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartadmin.modules.system.mapper.SysMenuMapper">

    <resultMap id="SysMenuResult" type="com.smartadmin.modules.system.entity.SysMenu">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="permission" column="permission"/>
        <result property="apiUrl" column="api_url"/>
        <result property="apiMethod" column="api_method"/>
        <result property="icon" column="icon"/>
        <result property="sort" column="sort"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 根据角色 ID 列表查询菜单 -->
    <select id="selectMenusByRoleIds" resultMap="SysMenuResult">
        SELECT DISTINCT m.*
        FROM sys_menu m
                 INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE m.status = 1
          AND rm.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        ORDER BY m.sort ASC
    </select>

    <!-- 根据用户 ID 查询菜单权限 -->
    <select id="selectMenuPermissionsByUserId" resultMap="SysMenuResult">
        SELECT DISTINCT m.*
        FROM sys_menu m
                 INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
                 INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE m.status = 1
          AND ur.user_id = #{userId}
        ORDER BY m.sort ASC
    </select>

</mapper>